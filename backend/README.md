# Backend Directory

حالياً النظام يعمل بشكل كامل من خلال المتصفح (Frontend Only) ويحفظ البيانات في الـ LocalStorage.

هذا المجلد مخصص لإضافة أي كود Backend في المستقبل (مثل Node.js, Python, PHP) إذا قررت ربط النظام بقاعدة بيانات سحابية.
